# Retained lifecycle scenario directory

The directory name identifies scenarios introduced in v0.3.6. The checked-in records/read-back signatures are regenerated against the current v0.3.7 tool text and Profiles. They are synthetic, not live transactions. `python appendix/tests/rebuild_lifecycle_examples.py` regenerates them; validation verifies their schemas and signatures at their declared synthetic time.

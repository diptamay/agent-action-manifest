# Demo trust root

Ed25519 key pair used ONLY to sign the example Profiles and run the tests. Never use it for anything real.

- `demo-profile-signing.key.hex` — private key (raw, hex)
- `demo-profile-signing.pub.hex` — public key (raw, hex); configure as trust root `key_id = demo-profile-signing-2026`
